#include "main.h"

/**
 * _islower - checks function for lower case
 *
 * @c: the alphabet to be checked in the function
 *
 * Return: Always 0.
 *
 */

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
